---
title: "Reporte proceso Anótate en la Lista (AEL) 2026 \\\ \\vspace{0.5em} \\Large SLEP `r params$slep`"
author: "División de Desarrollo Educativo"
date: "`r format(Sys.Date(), '%B %Y')`"
lang: es
format:
  pdf:
    latex-auto-install: false
    pdf-engine: xelatex
    keep-tex: false
    number-sections: true
fontsize: 12pt
geometry:
  - top=1.5cm
  - bottom=3cm
  - left=3cm
  - right=3cm
  - headsep=0cm
  - footskip=0.5cm
mainfont: Calibri Light
sansfont: Calibri Light
colorlinks: true
linkcolor: black
urlcolor: colorDEP
params:
  slep: nombre_slep
  glosario: null
  n_ee: null
  n_ee_atrasados: null
  n_ee_sin_cuenta: null
  tasa_cumplimiento: null
  dias_sin_mov: null
  listado_cuentas: null
  vas: null
  data_fig_1: null
  graf_lineas: null
  ael_actual: null
header-includes:
  - \renewcommand{\contentsname}{Tabla de contenidos}
  - \renewcommand{\tablename}{Tabla}
  - \usepackage{array}
  - \usepackage{booktabs}
  - \usepackage{caption}
  - \usepackage{titlesec}
  - \usepackage{titling}
  - \usepackage{float}
  - \usepackage{graphicx}
  - \usepackage{longtable}
  - \usepackage{multirow}
  - \usepackage{tabularray}
  - \usepackage{pdflscape}
  - \usepackage{tabularx}
  - \usepackage{xcolor}
  - \usepackage[most]{tcolorbox}
  - \pretitle{\begin{center}\LARGE\sffamily\color{titulo}}
  - \posttitle{\par\vspace{-0.8em}\end{center}}
  - \preauthor{\begin{center}\small\sffamily}
  - \postauthor{\par\vspace{-0.6em}\end{center}}
  - \predate{\begin{center}\footnotesize\sffamily}
  - \postdate{\par\end{center}}
  - \usepackage{fancyhdr}
  - \def\logopath{DEP_grafica_color.png}
  - \pagestyle{fancy}
  - \definecolor{titulo}{HTML}{0F4761}
  - \setlength{\headheight}{65pt}
  - \setlength{\droptitle}{-1em}
  - \fancyhf{}
  - \fancyhead[R]{\raisebox{0.45cm}{\includegraphics[height=2cm]{\logopath}}}
  - \addtolength{\headwidth}{2cm}
  - \fancyheadoffset[R]{2cm}
  - \renewcommand{\headrulewidth}{0pt}
  - \fancyfoot[R]{\color{colorDEP}\textbf{\thepage}}
  - \fancyfootoffset[R]{1cm}
  - \titleformat{\section}{\color{titulo}\normalfont\Large\bfseries\sffamily}{\thesection}{1em}{}
  - \titleformat{\subsection}{\color{titulo}\normalfont\large\bfseries\sffamily}{\thesubsection}{1em}{}
  - \titleformat{\subsubsection}{\color{titulo}\normalfont\normalsize\bfseries\sffamily}{\thesubsubsection}{1em}{}
  - \definecolor{colorDEP}{HTML}{0f69b4}
  - \makeatletter
  - \let\ps@plain\ps@fancy
  - \makeatother
---

```{r}
#| label: Paquetes
#| include: false

Sys.setlocale("LC_TIME", "es_ES.UTF-8")
pacman::p_load(
    tidyverse,
    data.table,
    openxlsx,
    janitor,
    kableExtra,
    fontawesome,
    ggrepel
)
```

```{r config_tablas}
#| message: false
#| echo: false

#Ajustes y preparación de BD
options(kableExtra.latex.load_packages = FALSE)
require(kableExtra)
options(knitr.table.format = "latex")
options(knitr.kable.NA = '-')
```

# Estimadas Subdirecciones UATP y Encargadas/os SAE:

Junto con saludarlos(as), desde la Dirección de Educación Pública ponemos a su disposición el informe sobre la gestión de listas de espera de matrícula en establecimientos de educación pública. Este documento busca facilitar información actualizada y analizada que oriente la toma de decisiones informadas en cada territorio.

La no gestión oportuna de las listas de espera constituye una situación que afecta directamente las trayectorias educativas de niñas, niños y jóvenes, oponiéndose a los principios y orientaciones de la Estrategia Nacional de Educación Pública, y debilitando la valoración de la educación. 

Garantizar la continuidad educativa y el derecho a aprender requiere de una acción activa de los establecimientos en distintas dimensiones, entre ellas, actualizar sus listas de espera, lo que promueve el acceso y permanencia de las y los estudiantes en el sistema educativo.

Desde la Dirección de Educación Pública, reafirmamos nuestro compromiso con el fortalecimiento de la educación pública y con el acompañamiento a las comunidades educativas. A través de la red de encargados SAE y la entrega de este y otros informes, buscamos contribuir a una gestión informada y a la toma de decisiones basadas en datos que resguarden las trayectorias educativas continuas y significativas.

Agradecemos el compromiso y los esfuerzos que realizan los equipos educativos por garantizar el acceso, la asistencia y la revinculación de sus estudiantes, avanzando juntos hacia una educación pública más inclusiva, equitativa y de calidad.

\begin{center}
Dirección de Educación Pública.
\end{center}

# Sobre el proceso de Anótate en la Lista

Este reporte busca entregar información respecto del proceso de regularización de matrícula a través de la plataforma **Anótate en la lista (AEL)**, el que se encuentra asociado al **Sistema de Admisión Escolar (SAE)** y que funciona de forma paralela a los procesos de admisión principal y complementarios. El **AEL** hoy representa la **única** forma de obtener una matrícula durante el año en curso, hasta antes de las matrículas 2027. Los estudiantes que participan de este proceso, lo hacen por diversos motivos, desde no tener un establecimiento a comienzos del año escolar, cambios de comuna de residencia, u otros motivos. Por lo anterior, es que la gestión de las vacantes disponibles, permiten proteger las trayectorias educativas de los estudiantes.

En este documento podrá encontrar información resumida y agregada a nivel de establecimiento, gráficos con el comportamiento en el tiempo de las **vacantes sin asignar**, tablas con el listado de establecimientos que no poseen cuentas activas para la gestión del AEL, listados de los establecimientos que deberían ser priorizados para mejorar la gestión y eficiencia del proceso **Anótate en la lista** debido a altas listas de espera o una gran cantidad de días sin gestión de vacantes sin asignar.

La información disponible en este documento se basa únicamente en los establecimientos participantes del SAE, incluyendo los niveles desde Prekínder hasta 4° medio, excluyendo las modalidades de educación especial, artística, EPJA, Salas Cunas y Jardines Infantiles.
\vspace{3mm}

::: callout-tip
## Importante

*Este reporte contiene datos de los cuerpos directivos de los establecimientos e información administrativa sensible pensada en apoyar la gestión del SLEP para garantizar el derecho a la educación de los estudiantes y no constituye un indicador de rendición de cuentas, por lo que los datos deben ser tratados con los resguardos de seguridad de la información necesarios*
:::

\newpage
# Metodología
\vspace{-3mm}
## Glosario de indicadores
\vspace{-5mm}
A continuación se presenta un glosario con las variables utilizadas y sus respectivas definiciones:
\vspace{-1mm}
```{r tabla_glosario}
#| echo: false
#| message: false
#| warning: false
#| results: "asis"

glosario <- params$glosario
glosario <- as.data.frame(glosario)

tbl_glosario <- glosario %>%
    kbl(
        booktabs = TRUE,
        longtable = FALSE,
        linesep = "\\addlinespace"
    ) %>%
    kable_styling(
        latex_options = c("HOLD_position"),
        font_size = 10,
        full_width = TRUE
    ) %>%
    column_spec(1, bold = TRUE, color = "#0F4761", width = "4cm") %>%
    column_spec(2, width = "11cm") %>%
    row_spec(0, bold = TRUE, color = "#0F4761", background = "white")

cat(knitr::knit_print(tbl_glosario))
```
\vspace{-3mm}
## Cálculo de vacantes sin asignar
\vspace{-3mm}
Las vacantes sin asignar corresponden al indicador utilizado por la DEP para monitorear la cantidad de estudiantes que pueden matricularse en un establecimiento en un período determinado del tiempo. Este indicador permite la agregación por establecimiento, nivel, comuna y SLEP, además de ser de fácil monitoreo y comunicación.

A continuación se presenta el cálculo de las **Vacantes Sin Asignar** (VAS) y un ejemplo concreto.
$$
\text{Vacantes sin Asignar (VAS)} = \text{mínimo}\{\text{Vacantes disponibles, Lista de espera}\}
$$

En el siguiente ejemplo, se presentan dos escenarios que dan cuenta del cálculo de las VAS disponibles.
$$
\text{Vacantes sin asignar (VAS)} =\begin{cases}
    \text{10 Vacantes y 5 estudiantes en Lista de espera} & \longrightarrow \text{5 VAS} \\
    \text{10 Vacantes y 20 estudiantes en Lista de espera} & \longrightarrow \text{10 VAS}
  \end{cases}
$$

1.  En el **primer escenario**, las vacantes sin asignar corresponden al total de estudiantes en Listas de espera, que es menor a la cantidad de vacantes disponibles. En el Establecimiento aún quedarían 5 vacantes disponibles.

2.  En el **segundo escenario**, las vacantes sin asignar corresponden al total de vacantes disponibles, dado que es menor que la lista de espera y no se pueden incorporar más estudiantes que la oferta disponible.

# Estado del territorio

\vspace{-5mm}
\begin{tcbitemize}[raster columns=2, raster equal height, raster column skip=1em,
  raster row skip=1em, enhanced, colback=white, boxrule=0.5pt,
  valign=center, halign=center, arc=3mm]

  \tcbitem[colframe=blue!70!black, title=Establecimientos en SAE, colbacktitle=blue!70!black, fonttitle=\bfseries\sffamily, halign title=center]
  {\Large \textbf{\textsf{`r params$n_ee`}}}

  \tcbitem[colframe=yellow!60!black, title=Establecimientos atrasados, colbacktitle=yellow!60!black, fonttitle=\bfseries\sffamily, halign title=center]
  {\Large \textbf{\textsf{`r params$n_ee_atrasados`}}}

  \tcbitem[colframe=green!60!black, title=Vacantes sin asignar, colbacktitle=green!60!black, fonttitle=\bfseries\sffamily, halign title=center]
  {\Large \textbf{\textsf{`r params$vas`}}}

  \tcbitem[colframe=red!70!black, title=Establecimientos sin cuenta activa (al `r params$fecha_corte`), colbacktitle=red!70!black, fonttitle=\bfseries\sffamily, halign title=center]
  {\Large \textbf{\textsf{`r params$n_ee_sin_cuenta`}}}
\end{tcbitemize}

## Establecimientos sin cuentas activas

Para una correcta gestión del **Anótate en la lista** es necesario que todos los establecimientos del SLEP `r params$slep` cumplan con mantener la información de contacto de Directores/as y Encargadas/os SAE actualizada en **SIGE**.

La información de contacto es utilizada por el Mineduc para la creación de los usuarios, a través del envío de un mail automático a ambos actores, por lo que **si esta información se encuentra desactualizada en SIGE no es posible la creación de la cuenta**, y en consecuencia, no es posible gestionar las listas de espera, afectando el derecho a la educación de los postulantes.

```{r}
#| echo: false
#| message: false
#| warning: false

aux_cuentas <- params$n_ee_sin_cuenta
if (aux_cuentas != 0) {
    porcentaje <- round(params$n_ee_sin_cuenta * 100 / params$n_ee, 1)

    knitr::asis_output(paste0(
        "
::: {.callout-important title=\"Alerta de establecimientos sin cuentas activas al ",
        params$fecha_corte,
        ".\"}
En su SLEP actualmente un **",
        porcentaje,
        "%** de sus establecimientos no poseen ninguna cuenta activa (Director/Encargado SAE).
:::
"
    ))
}
```

A continuación se presenta la lista de establecimientos que no tienen cuenta activa para gestionar el AEL y su respectiva cantidad de estudiantes en lista de espera.

1.  **Mail Director/Encargado SAE**: Mail registrado en SIGE al cual se envió el enlace para la creación de usuario.
2.  **Fecha envío correo Director/Encargado SAE**: Fecha en que se envió el correo por parte de Mineduc para la creación de la cuenta.

**Nota 1:** Si la información de correo aparece en blanco, es porque en SIGE no hay un correo registrado para ese cargo.

**Nota 2:** Es posible que el correo enviado por Mineduc se aloje en la carpeta SPAM.


```{r cuentas_slep}
#| echo: false
#| message: false
#| warning: false
#| results: "asis"

listado_cuentas_table <- params$listado_cuentas
listado_cuentas_table <- as.data.frame(listado_cuentas_table)

if (nrow(listado_cuentas_table) == 0) {
    cat(
        "
::: {.callout-tip title='Establecimientos con cuentas'}
Actualmente, todos los establecimientos del SLEP cuentan con al menos un usuario activo en el sistema.
:::
"
    )
} else {
    cat("\\begin{landscape}\n")

    colnames(listado_cuentas_table) <- c(
        "Establecimiento",
        "Lista de espera",
        "Mail Director",
        "Mail encargado SAE",
        "Fecha correo Director",
        "Fecha correo SAE"
    )

    tbl <- listado_cuentas_table %>%
    arrange(-`Lista de espera`) %>%
    kbl(
        caption = "Listado de EE sin cuentas activas (Ni director Ni encargado SAE).",
        booktabs = TRUE,
        longtable = TRUE,
        linesep = "",
        align = c("l", "c", "l", "l", "c", "c") 
    ) %>%
    kable_styling(
        latex_options = c("HOLD_position", "repeat_header"),
        font_size = 9
    ) %>%
    column_spec(1, width = "4cm") %>%
    column_spec(2, bold = TRUE, width = "1.5cm") %>%
    column_spec(3, width = "7cm") %>%
    column_spec(4, width = "7cm") %>%
    column_spec(5, width = "2cm") %>%
    column_spec(6, width = "2cm") %>%
    row_spec(0, bold = TRUE, color = "#0F4761", background = "white") %>%
    add_footnote(
        "La información actualizada se encuentra en el visor compartido",
        notation = "symbol",
        escape = FALSE
    )

    cat(knitr::knit_print(tbl))
    cat("\\end{landscape}\n")
}
```



\newpage
## Estado de las Vacantes sin Asignar

::: callout-caution
# Establecimientos atrasados en la gestión de AEL

En su SLEP actualmente hay un total de **`r params$n_ee_atrasados` establecimientos atrasados**, los que representan un total de **`r params$vas` vacantes sin asignar**.
:::

En la siguiente Figura se aprecia la distribución de los **36 SLEP** en régimen al 2026, donde el círculo de color diferente es la ubicación de su SLEP según la **tasa de cumplimiento** (eje horizontal) y las vacantes sin asignar (VAS, eje vertical)

```{r fig_scatter}
#| echo: false
#| message: false
#| warning: false
#| fig-align: center

datos_fig_1 <- params$data_fig_1
datos_fig_1 <- as.data.frame(datos_fig_1)
max_fig_1 <- ceiling(max(datos_fig_1$Vacantes.sin.asignar) / 50) * 50

datos_fig_1 %>%
    mutate(
        nombre_slep = if_else(nombre_slep == params$slep, nombre_slep, "")
    ) %>%
    ggplot(aes(
        x = Tasa.de.cumplimiento,
        y = Vacantes.sin.asignar,
        fill = nombre_slep
    )) +
    geom_point(
        aes(color = nombre_slep, fill = nombre_slep),
        size = 4,
        alpha = 0.5,
        shape = 21,
        show.legend = FALSE
    ) +
    theme_minimal() +
    geom_text_repel(
        aes(label = nombre_slep),
        color = "black",
        size = 11 / .pt,
        point.padding = 0.1,
        box.padding = 0.6,
        min.segment.length = 0,
        max.overlaps = 1000,
        seed = 7654
    ) +
    scale_x_continuous(n.breaks = 6) +
    scale_y_continuous(
        limits = c(0, max_fig_1),
        breaks = seq(0, max_fig_1, length.out = 6),
        expand = c(0.03, 0.04)
    ) +
    labs(
        x = "Tasa de cumplimiento(%)",
        y = "Total Vacantes sin asignar",
        title = "Distribución de los SLEP según % de cumplimiento y VAS"
    )
```
\newpage
## Distribución de los establecimientos atrasados

En esta sección se presenta la distribución de los establecimientos atrasados según la cantidad de vacantes sin asignar (VAS). De esta forma es posible identificar si se tiene pocas VAS pero muchos establecimientos atrasados, o muchas VAS pero concentradas en pocos establecimientos.

```{r fig_histogram}
#| echo: false
#| message: false
#| warning: false
#| fig-width: 8
#| fig-height: 5
#| fig-align: center

listado_ael_rbd <- params$ael_actual
listado_ael_rbd <- as.data.frame(listado_ael_rbd)

if (nrow(listado_ael_rbd) == 0) {
    knitr::asis_output(paste0(
        "
::: {.callout-tip title=\"✅ Establecimientos sin vacantes pendientes de asignar\"}
Actualmente, todos los establecimientos del SLEP han gestionado sus vacantes pendientes.
:::"
    ))
} else if (nrow(listado_ael_rbd) == 1) {
    knitr::asis_output(paste0(
        "
::: {.callout-warning title=\"Existe un establecimiento con vacantes pendientes de asignar\"}
Actualmente, existe solo un establecimiento con vacantes pendientes de asignar.
:::"
    ))
} else {
    colnames(listado_ael_rbd) <- c(
        "Comuna",
        "RBD",
        "Establecimiento",
        "Vacantes sin Asignar",
        "Niveles Atrasados",
        "Niveles Ofrecidos",
        "Promedio días sin movimiento en AEL"
    )

    mediana_vas <- listado_ael_rbd %>%
        pull(`Vacantes sin Asignar`) %>%
        median()

    datos_densidad <- listado_ael_rbd %>%
        pull(`Vacantes sin Asignar`) %>%
        density()

    altura_max <- max(datos_densidad$y)

    listado_ael_rbd %>%
        ggplot(aes(x = `Vacantes sin Asignar`)) +
        geom_density(fill = "#0F69B4", color = "#0F69B4", alpha = 0.7) +
        geom_vline(
            xintercept = mediana_vas,
            linetype = "dashed",
            color = "#084A7A",
            linewidth = 0.8
        ) +
        geom_label_repel(
            data = data.frame(x = mediana_vas, y = altura_max * 0.85),
            aes(
                x = x,
                y = y,
                label = paste0(
                    "El 50% de los EE tiene\nmenos de ",
                    round(mediana_vas, 1),
                    " vacantes sin asignar"
                )
            ),
            nudge_x = 5,
            color = "black",
            size = 11 / .pt,
            box.padding = 0.5,
            point.padding = 0.3,
            segment.color = "#084A7A",
            segment.size = 0.5,
            arrow = arrow(length = unit(0.02, "npc"), type = "closed"),
            min.segment.length = 0
        ) +
        theme_minimal() +
        theme(
            legend.position = "none",
            plot.title = element_text(size = 14),
            panel.grid = element_blank()
        ) +
        labs(title = "Densidad de EE según VAS", y = "Densidad")
}
```

\newpage
## Comportamiento Histórico

En esta sección se presenta el comportamiento de las vacantes sin asignar (VAS) durante el periodo de 29 de diciembre hasta el 05 de enero para cada comuna del territorio, los datos se obtuvieron con una frecuencia semanal, y se **actualizarán con una frecuencia semanal**.

::: callout-warning
# Antecedentes de fiscalización

La **Superintendencia de Educación** compartió a través del oficio N\° 492 del **26 de marzo de 2025** las instrucciones y consecuencias de la faltas de gestión de las listas de espera. Lo hizo anteriormente en el oficio N 164 el 27 de enero de 2025.
:::

```{r fig_lineas_comunas}
#| echo: false
#| message: false
#| warning: false
#| out-width: "100%"
#| fig-height: 6

temp_2 <- params$graf_lineas
temp_2 <- as.data.frame(temp_2)
temp_2 <- temp_2 %>%
    mutate(fecha_corte_info = as.Date(fecha_corte_info, format = "%d-%m-%Y"))

temp_2 %>%
    ggplot(aes(x = fecha_corte_info, y = vas)) +
    # Background layer: all grey lines (one per comuna2 group), no color aesthetic
    geom_line(
        data = temp_2,
        aes(group = comuna2),
        color = "grey",
        linewidth = 0.5,
        alpha = 0.5
    ) +
    # Highlighted layer: colored line for the faceted comuna
    geom_line(
        aes(group = comuna),
        color = "#69b3a2",
        linewidth = 1.2
    ) +
    theme_minimal() +
    theme(
        legend.position = "none",
        plot.title = element_text(size = 14),
        panel.grid = element_blank()
    ) +
    labs(
        title = "Vacantes sin asignar durante el 2026, por comuna y día",
        subtitle = "Datos semanales",
        x = NULL,
        y = "Vacantes sin Asignar (VAS)"
    ) +
    facet_wrap(~comuna, ncol = 2)
```

### Variación vacantes sin Asignar

En esta sección se presenta el estado **actual** de las vacantes sin asignar por comuna.

```{r tabla_variacion_vas}
#| echo: false
#| message: false
#| warning: false

temp_3 <- params$graf_lineas
temp_3 <- as.data.frame(temp_3)

temp_3 <- temp_3 %>%
    mutate(
        fecha_corte_info = as.Date(fecha_corte_info, format = "%d-%m-%Y")
    ) %>%
    slice_max(order_by = fecha_corte_info, by = comuna, n = 2) %>%
    mutate(fecha_corte_info = format(fecha_corte_info, "%A %d %B"))

temp_3 <- temp_3 %>%
    summarize(
        `total VAS` = sum(vas, na.rm = TRUE),
        .by = c(comuna, fecha_corte_info)
    ) %>%
    pivot_wider(
        names_from = fecha_corte_info,
        values_from = `total VAS`
    ) %>%
    rename(Comuna = comuna)

temp_3 <- temp_3 %>%
    mutate(`Variación entre mediciones` = .[[2]] - .[[3]])

temp_3_totales <- temp_3 %>%
    adorn_totals(where = "row", fill = "Total")

n_filas <- nrow(temp_3_totales)

temp_3_totales %>%
    kbl(
        caption = "Vacantes sin asignar por comuna",
        booktabs = TRUE,
        longtable = FALSE,
        linesep = "",
        align = c("l", "c", "c", "c")
    ) %>%
    kable_styling(
        latex_options = c("striped", "HOLD_position"),
        stripe_color = "#F0F4FA",
        font_size = 10,
        full_width = TRUE
    ) %>%
    column_spec(1, bold = TRUE, color = "#0F4761") %>%
    row_spec(0, bold = TRUE, color = "#0F4761", background = "#0F69B4") %>%
    row_spec(n_filas, bold = TRUE, color = "#0F4761") %>%
    add_footnote(
        "Fuente: Visor Anótate en la lista, 2026.",
        notation = "symbol",
        escape = FALSE
    )
```

### Listado de gestión.

En esta sección se presenta el listado de establecimientos según criticidad, ordenados por la cantidad de vacantes sin asignar.

```{r listado_rbds_gestion}
#| echo: false
#| message: false
#| warning: false

listado_ael_rbd <- params$ael_actual
listado_ael_rbd <- as.data.frame(listado_ael_rbd)

if (nrow(listado_ael_rbd) == 0) {
    knitr::asis_output(paste0(
        "
::: {.callout-tip title=\"✅ Establecimientos sin vacantes pendientes de asignar\"}
Actualmente, todos los establecimientos del SLEP han gestionado sus vacantes pendientes.
:::"
    ))
} else {
    colnames(listado_ael_rbd) <- c(
        "Comuna",
        "RBD",
        "Establecimiento",
        "VAS",
        "Niv. Atrasados",
        "Niv. Ofrecidos",
        "Días sin mov."
    )

    listado_ael_rbd %>%
        arrange(desc(VAS)) %>%
        kbl(
            caption = "Estado actual del AEL por Establecimiento. Ordenado según Vacantes sin asignar (VAS).",
            booktabs = TRUE,
            longtable = TRUE,
            linesep = "",
            align = c("l", "c", "l", "c", "c", "c", "c")
        ) %>%
        kable_styling(
            latex_options = c("striped", "HOLD_position", "repeat_header"),
            stripe_color = "#F0F4FA",
            font_size = 9,
            full_width = TRUE
        ) %>%
        column_spec(1, width = "2cm") %>%
        column_spec(2, width = "1cm") %>%
        column_spec(3, width = "5cm") %>%
        column_spec(4, bold = TRUE, width = "1.2cm") %>%
        column_spec(5, width = "1.5cm") %>%
        column_spec(6, width = "1.5cm") %>%
        column_spec(7, width = "1.5cm") %>%
        row_spec(0, bold = TRUE, color = "#0F4761", background = "#0F69B4") %>%
        add_footnote(
            "La información actualizada se encuentra en el visor compartido",
            notation = "symbol",
            escape = FALSE
        )
}
```
